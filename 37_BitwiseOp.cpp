#include <iostream>
using namespace std;
int main(){
    //logical operator or bitwise operator
    /* 
    & and 
    | or 
    ^ xor
    << left shift (it shifts left binaray data of n number by i places){n<<i} [a<<b = a*2 power b]
    >> rigth shift (it shifts right binary data of n number by i places) {n>>i} [a>>b = a/2 power b]
    */
}